# دليل سريع - نظام تسجيل الزيارات

## الخطوات السريعة

### 1. إعداد قاعدة البيانات

عدّل ملف `.env`:

```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=intellijapp
DB_USERNAME=root
DB_PASSWORD=
```

### 2. إنشاء قاعدة البيانات في MySQL

```sql
CREATE DATABASE intellijapp CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 3. تشغيل Migration

```bash
php artisan migrate
```

### 4. جاهز!

النظام الآن يسجل جميع الزيارات تلقائياً! 🎉

## عرض الإحصائيات

افتح المتصفح واذهب إلى:
```
http://localhost:8000/admin/visits
```

## ما يتم تسجيله

- ✅ عنوان IP
- ✅ مصدر الزيارة (Google, Facebook, Direct, etc.)
- ✅ الصفحة المزورة
- ✅ نوع الجهاز (Mobile, Tablet, Desktop)
- ✅ نوع المتصفح
- ✅ نظام التشغيل
- ✅ التاريخ والوقت

## API للإحصائيات

```
GET /admin/visits/stats
```

يرجع JSON مع جميع الإحصائيات.

## ملاحظة

النظام يعمل تلقائياً - لا حاجة لأي إعداد إضافي!
